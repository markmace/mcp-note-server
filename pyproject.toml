[project]
name = "mcp-note-server"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
    "mcp>=1.8.0",
    "python-multipart>=0.0.12",
]

[tool.uv]
dev-dependencies = []
